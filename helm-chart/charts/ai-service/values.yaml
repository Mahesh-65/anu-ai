name: ai-service
replicaCount: 1
rollingUpdate: { type: RollingUpdate, maxUnavailable: 0, maxSurge: 1 }
image: { repository: organistation-ai, tag: v1.0.0, pullPolicy: IfNotPresent }
service: { port: 8000, targetPort: 8000 }
resources:
  limits: { cpu: "1", memory: 2Gi }
  requests: { cpu: 500m, memory: 1Gi }
