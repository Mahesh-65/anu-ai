# Copy to .env at repo root:  cp .env.example .env
# Used by docker-compose.yml

# Required — must be the same value gateway and auth use
JWT_SECRET=change-this-to-a-strong-shared-secret

# Optional — AI service (leave empty to use local fallback mode)
GEMINI_API_KEY=
GROQ_API_KEY=
